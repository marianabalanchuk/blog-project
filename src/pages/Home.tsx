import ArticlesList from 'components/Articles/ArticlesList'

type Props = {}
const Home = (props: Props) => {
    return (
        <div>
            <ArticlesList />
        </div>
    )
}
export default Home
